# Research dossier — how this skill was verified

Every claim in SKILL.md traces back to something read or run on a live machine
(2026-07-25). Five parallel readers each covered one source and reported findings,
verified commands, and pitfalls; three adversarial critics then tried to break the
draft (first-timer walkthrough, command-truth against the live binaries, blast-radius).

Kept because the expensive part of this skill is not the prose — it is knowing WHICH
claims were checked against a real system and which were inherited from older docs.
When maw-rs changes, re-check against this rather than trusting the summary.

---

### SOURCE: codex-lead
FINDINGS:
- SKILL.md is a single 169-line file at /Users/nat/.claude/skills/codex-lead/SKILL.md — no references/, no scripts/, no supporting files. `ls -la` shows only SKILL.md (8.7K).
- Frontmatter (lines 2-4): `name: codex-lead`, a long quoted `description`, and `argument-hint: "up [N] | dispatch <task> | peek | scale N | down"`. Note the argument-hint verbs (up/dispatch/peek/scale/down) are NEVER defined as dispatchable modes anywhere in the body — the body is a linear 8-section narrative, so the hint is aspirational, not implemented.
- GOLDEN RULES (lines 13-15, verbatim): "Golden rules: **dispatch ONLY via `maw hey` (foreground)**, NEVER `maw team send`/SendMessage on omx (silent no-op — that's a claude-only file inbox). The lead **reviews + merges, never writes code**. Define done-criteria up front; peek on a cadence; no-gap dispatch." VERIFIED CORRECT: crates/maw-cli/src/core_impl/team_messaging.rs:103 writes to `team_paths(team).tool_dir.join("inboxes").join(format!("{agent}.json"))` — pure file write, zero tmux injection. By contrast `maw hey --help` states "default: write receiver inbox and inject into the target pane".
- CHARTER FORMAT v1 (§1, lines 17-22): Required keys `name`, `project` (called MANDATORY — "without it worktrees land wrong"), `defaults: {worktree: true}`, `members` (each: `role`, `name`, `engine`, `branch`, `prompt`). Lead is `engine: claude, worktree: false, name: <tmux window>`. Coders are `engine: omx, branch: agents/<team>-coder-N`.
- CHARTER FORMAT v2 (§8, lines 97-129) EXPLICITLY CONTRADICTS §1 and says so: "v2 contract (verified live, NOT the old defaults.worktree:true schema): - NO defaults.worktree block - every member declares its own worktree: and branch: - lead: worktree:false, branch:alpha". So the file ships two mutually exclusive charter schemas with no guidance on which to use when. §1 is the stale one by the file's own admission.
- v2 charter template (lines 105-129) verbatim heredoc keys: `name`, `project: <org>/<repo>`, `session: <tmux-session>`, `engines:` map, `members:` list, and a trailing `lifecycle:` block with `worktree: true` and `merge_on_shutdown: false`. Engine line verbatim: `omx-N: "bun $HOME/.claude/skills/oracle-team/scripts/codex-setup.ts N && CODEX_HOME=$PWD/.codex OMX_AUTO_UPDATE=0 omx --direct --madmax"`
- codex-setup.ts EXISTS and its own header docs match the skill's engine line character-for-character. /Users/nat/.claude/skills/oracle-team/scripts/codex-setup.ts lines 14-16: "Usage (in a maw engine command, run from the coder's worktree): bun ~/.claude/skills/oracle-team/scripts/codex-setup.ts <pool-index> && CODEX_HOME=$PWD/.codex OMX_AUTO_UPDATE=0 omx --direct --madmax". Script comment also justifies why it does NOT launch omx: "a forked omx loses the terminal → exit 1".
- Pool indices are real: `ls ~/.codex-team/` returns dirs `1 2 3 4 5 6` plus `hermes`. So `codex-setup.ts N` for N in 1..6 is valid; the skill's example `N = 5` resolves.
- DISPATCH MECHANICS (§4, lines 49-54) verbatim command: `maw hey <sess>:coder-N "<task + explicit done-criteria>"   # foreground; reaches omx (delivers + submits)`. Plus the false-negative rule: "'may not have submitted / still shows pending input' = **FALSE NEGATIVE**. Confirm with `maw peek`; only `maw send-enter` if the peek shows your text actually sitting unsent." And: "One precise minimal task per coder. State done-criteria (tests pass / build green / behavior verified)."
- PEEK LOOP CADENCE (§5, lines 57-63) is 5 numbered steps: 1) Dispatch with clear done-criteria. 2) "Every **~15–20 min**: `maw peek <sess>:coder-N` each coder — who's working / idle / stalled." 3) "Stalled → nudge with the **exact blocker**, not 'how's it going'." 4) "Done → review + merge + **NO-GAP DISPATCH** (next task in the same confirm message)." 5) "Keep a task queue so the next dispatch fires instantly." Quick views listed: `maw team status <team>`, `maw activity`.
- TOKEN ROTATION (§6, lines 65-73): "omx coders route through the `maw zai` gateway. `fill_first` drains key #1, auto-overflows to the next on 429/quota — **that overflow IS the rotation; no extra setup.**" VERIFIED: ~/.maw/plugins/zai/index.ts:134 prints "fill_first: drains <name> fully, then overflows down the chain"; index.ts:118 comment "active head = first non-cooling entry by priority (fill_first rule)"; index.ts:130 "overflow when full/429". Live `maw zai status` shows a 4-key pool (GLM_API_KEY env + api-key-2/3/4 manual).
- BOOT-PITFALL HEAL TABLE (§3, lines 38-42) — the three rows verbatim: empty shell `❯` → `maw run <sess>:coder-N "OMX_AUTO_UPDATE=0 omx"`; codex update menu (1.Update / 3.Skip…) → `maw send-text <sess>:coder-N "3"`; text stuck in composer → `maw send-enter <sess>:coder-N`. Root cause given: "A fresh worktree has no `.envrc` → `OMX_AUTO_UPDATE=0` doesn't take → omx self-updates → drops to a **shell** (`❯`) or a codex **update menu**". Post-heal step: re-send contract with `maw hey <sess>:coder-N "<coder prompt>"` then peek that "`Context …% left` dropped below 100% (= ingested)".
- §3 also correctly warns "Use maw verbs only — raw `tmux send-keys` is safety-hook blocked."
- STALE — issue #658 is CLOSED/FIXED. §8's "⚠️ Known bug: maw-rs #658 — last-member `agents/` prefix gets stripped" (lines 142-153) says "Three known workarounds (until #658 ships)". `gh issue view 658` returns state CLOSED, closedAt 2026-07-23T15:30:18Z. Fix is commit 9007f783 "fix(team): preserve charter member worktrees across sections (#664)", PR #664 MERGED, dated Thu Jul 23 2026. Installed binary is maw-rs v26.7.25-alpha.1755 (fd82a9b) built 2026-07-25 — after the fix. All three workarounds (coder-before-lead ordering, symlink+gitignore, skip team up) are now unnecessary.
- The #664 fix body confirms the exact failure the skill's own v2 template would have triggered: "If a top-level section followed the final `members:` item, indented keys in that section could still be parsed as fields on the last member. A trailing `worktree:` line therefore clobbered only the last member's `worktree: agents/...`" — i.e. the skill's trailing `lifecycle: worktree: true` block WAS the trigger, which is why workaround #1 (coder listed before lead) was needed.
- STALE — `maw team down <team> --clean` DOES NOT EXIST. §7 line 78: "Teardown: `maw team down <team>` (worktrees clean up; `--clean` also deletes merged branches)." Actual parser at crates/maw-cli/src/core_impl/team_down.rs:40-55 accepts only `--all`, `--dry-run`, `--status`, `--keep <a,b>`; any other dash arg hits `return Err(format!("team down: unknown argument {value}"))`. Usage string is `usage: maw team down <team> [--all] [--keep <a,b>] [--dry-run] [--status]`. The branch-deleting flag elsewhere in maw is `--clean-branch` on `maw done` (worktree_finish.rs:6).
- BUG — bare `maw activity` (§5 line 63) is not runnable. It prints usage and does nothing: `usage: maw activity <pane> [--watch] [--json] [--stuck-only] [--window=<dur>] [--samples=N] [--sampler=peek|follow] | maw activity --all [...]`. Must be `maw activity --all`.
- VERIFIED — `maw wake <role> --session <sess> --no-attach --repo-path <abs>` (workaround #3, line 152) IS valid despite `maw wake --help` not listing `--session` or `--repo-path`. wake.rs:466 `"--parent" | "--session" => ...` and wake.rs:472 `"--repo-path" => ...`. Confirmed by wake.rs:2412 test comment: "`team up` passes `--repo-path <worktree>`; wake must accept it". team_up_apply.rs:149 builds exactly `["wake", identity, "--no-attach", "--session", session, "-e", engine]` + `["--repo-path", repo]`.
- VERIFIED — `maw team up <team> --dry-run` and `--only <a,b>` are real. team_up_apply.rs:4 usage string: `usage: maw team up <team> [--session <name>] [--members <roles>] [--only <a,b>] [--dry-run] [--status] [-e <engine>]`. Note `--only` takes a CSV (team_up_helpers.rs:92 `team_t3_csv`), and `--members <roles>` and `--status` exist but the skill never mentions them.
- §2's dry-run description "live=skip, missing=would wake" is INCOMPLETE — team_up_apply.rs:34-37 shows three states: `live`/`skipped` → skip, `dead` (without --force) → "resume in place", `missing` → "fresh wake". There is also a `--force` flag that kills the window first (team_t5b_kill_window). The skill never mentions the dead/resume path or --force.
- STALE REFERENCE (×2) — ting's `/codex-fleet` skill does not exist. Cited in the frontmatter description ("see ting's /codex-fleet for spawn/arrange/heal mechanics") and again at line 11: "For pure spawn/arrange/heal mechanics see ting's `~/.claude/skills/codex-fleet/SKILL.md`." `ls -d /Users/nat/.claude/skills/codex-fleet` → No such file or directory; `fd -t d codex-fleet` across ~/.claude and the Soul-Brews-Studio tree returns nothing.
- STALE REFERENCE — §1 line 22 says "Copy a known-good charter (e.g. `volt-codex2.yaml`)." `fd -HI volt-codex2.yaml` across /opt/Code/github.com/Soul-Brews-Studio finds nothing. The dangling pointer means §1 gives you a schema description with no working exemplar.
- `maw team preflight` takes a FILE PATH, not a team name: team_core.rs:221 `usage: maw team preflight <team.yaml|team.json>`. Same for load (team_core.rs:228). But `maw team up`/`down`/`status`/`send` take a team NAME and reject leading-dash args with "unsafe team name '<x>': leading dash rejected" — meaning `maw team up --help` errors with "team: unknown argument --help" and `maw team down --help` errors with the unsafe-name message. There is no per-subcommand help for team.
- POSSIBLE CONTRADICTION between §6 and §8's pool section: §6 says "omx coders route through the `maw zai` gateway" (GLM/Z.AI keys, 4-key pool), while §8's "Pool/account contention" (lines 163-169) says coders use `~/.codex-team/<N>` credential slots. These are two different auth paths for the same `omx` engine and the skill never reconciles them — a reader following §8's fast path gets codex-account rotation, NOT zai fill_first rotation, so §6's monitoring advice may not apply to a §8-spawned team.
- REPORT-BACK TARGET rule (lines 155-161) is a genuinely load-bearing gotcha and still accurate: "`<lead>` must be the **actual tmux window index or name** (check with `maw ls -v` or `tmux list-windows -t <session>`), which is often just the session's window **1**, not the string `\"lead\"` from the charter." `maw ls -v` verified working — it renders a TARGET column you can paste directly. `maw hey --help` confirms target forms include `<session>:<window>[.<pane>]` with the note "paste a TARGET from maw ls -v".
- POOL CONTENTION note (lines 163-169) verbatim claim: "Pools (`~/.codex-team/<N>`) are shared **credential** slots across every codex team on the machine, even though `codex-setup.ts` gives each coder a worktree-local `CODEX_HOME` (isolated state/locks)." Corroborated by codex-setup.ts header: "auth.json symlinked from the shared pool (reuse credential)" + "own SQLite/locks created fresh by omx → no fleet-wide collision".
- ANTI-PATTERNS section (lines 80-85), 3 items verbatim: "**Charter-before-verification breaks teams** — verify every charter task against live code (`ls`/`grep`) before finalizing, or coders block on their first task." / "**Account/token change → full lead-cycle pause** (not just stop new picks)." / "**omx fails to start** → `maw team up <team> --only coder-N` relaunch."
- §8 prerequisite step 0 (lines 93-95) — the repo must be non-empty BEFORE any worktree/spawn: "(worktree add fails on an unborn HEAD; coders need origin to push/PR against.)" Commands: `git log --oneline -1 || { git add README.md && git commit -m "Initial commit" && git push -u origin main; }` then `git checkout -b alpha && git push -u origin alpha && git checkout main`.
- §8 step 2 pre-seeds trust manually before team up ("SPAWN ONE FIRST — the golden rule"): `git worktree add agents/codex-1 -b agents/codex-1 origin/alpha` followed by `(cd agents/codex-1 && bun ~/.claude/skills/oracle-team/scripts/codex-setup.ts N && printf "\n[projects.\"$PWD\"]\ntrust_level=\"trusted\"\n" >> .codex/config.toml)`. Verification gate: "`maw peek <session>:codex-1` — must show the omx/Codex engine UI, not a shell or trust prompt".
- Provenance claims in the file: §intro "Proven on volt-codex2 (2026-06-24)"; §8 "Proven end-to-end on `codex-fanout` (2026-07-23): empty repo → spawn → probe PR → merge, in one sitting." The §8 date (2026-07-23) is the same day #658 was closed, which is consistent with §8 being the fresher half of the file — another signal that §1-§7 are the older layer.
VERIFIED COMMANDS:
- maw team up <team> --dry-run
- maw team up <team>
- maw team up <team> --only codex-1
- maw team up <team> --only coder-N
- maw team load ψ/teams/<team>.yaml --no-spawn
- maw team preflight ψ/teams/<team>.yaml
- maw team status <team>
- maw team down <team>
- maw team down <team> --dry-run
- maw team down <team> --keep lead
- maw peek <session>:codex-1
- maw peek <sess>:coder-N
- maw peek <sess>:coder-N --lines 200
- maw run <sess>:coder-N "OMX_AUTO_UPDATE=0 omx"
- maw send-text <sess>:coder-N "3"
- maw send-enter <sess>:coder-N
- maw hey <sess>:coder-N "<task + explicit done-criteria>"
- maw hey <session>:<lead-window-index> "done/blocked — <details>"
- maw zai status
- maw zai mon
- maw zai test
- maw activity --all
- maw activity --all --stuck-only
- maw ls -v
- tmux list-windows -t <session>
- maw wake <role> --session <sess> --no-attach --repo-path <abs-path-to-worktree>
- bun $HOME/.claude/skills/oracle-team/scripts/codex-setup.ts N && CODEX_HOME=$PWD/.codex OMX_AUTO_UPDATE=0 omx --direct --madmax
- git worktree add agents/codex-1 -b agents/codex-1 origin/alpha
- git log --oneline -1 || { git add README.md && git commit -m "Initial commit" && git push -u origin main; }
- git checkout -b alpha && git push -u origin alpha && git checkout main
PITFALLS:
- SHIP-BLOCKER: `maw team down <team> --clean` is documented at SKILL.md:78 but the flag does not exist. team_down.rs:48-49 hard-errors on any unrecognized dash arg ("team down: unknown argument --clean"), so teardown fails outright. FIX: drop `--clean` from the line. Real accepted flags are `[--all] [--keep <a,b>] [--dry-run] [--status]`. If the intent was deleting merged branches, that lives on a different verb: `maw done <window> --clean-branch`.
- SHIP-BLOCKER: bare `maw activity` at SKILL.md:63 is a no-op that only prints usage. FIX: change to `maw activity --all` (or `maw activity <pane>`). Add `--stuck-only` for the peek-loop use case — it directly serves step 2's "who's working / idle / stalled".
- STALE SECTION: the entire "⚠️ Known bug: maw-rs #658" block (SKILL.md:142-153) plus its three workarounds should be deleted or demoted to a historical note. Issue #658 closed 2026-07-23; PR #664 (commit 9007f783) merged the same day; the installed binary v26.7.25-alpha.1755 (fd82a9b, built 2026-07-25) contains it. Keeping it makes leads apply an unnecessary members-ordering constraint and stray symlinks. FIX: if you keep anything, keep only "put the coder before lead" as a harmless convention, and state it is no longer required on maw-rs >= v26.7.24.
- TWO CONTRADICTORY CHARTER SCHEMAS in one file. §1 (lines 17-22) prescribes `defaults: {worktree: true}`; §8 (line 98) prescribes "NO defaults.worktree block" and calls §1 "the old ... schema". A reader hitting §1 first builds a charter §8 declares obsolete. FIX: delete §1's schema prose and make the §8 v2 heredoc the single source of truth, or mark §1 explicitly as "legacy — do not use for new teams".
- TWO DANGLING FILE REFERENCES that will 404 for anyone following them: (a) `~/.claude/skills/codex-fleet/SKILL.md` — cited in the frontmatter description AND at line 11, does not exist anywhere under ~/.claude; (b) `volt-codex2.yaml` — cited at line 22 as the charter to copy, not present anywhere in /opt/Code/github.com/Soul-Brews-Studio. FIX: either vendor a known-good charter into the skill as `references/charter-v2.yaml` (the §8 heredoc already is one — promote it to a file) and delete the volt-codex2 pointer, or restore the codex-fleet skill. Shipping a skill whose two 'see also' pointers both dangle is worse than having none.
- FRONTMATTER PROMISES MODES THE BODY DOESN'T IMPLEMENT. `argument-hint: "up [N] | dispatch <task> | peek | scale N | down"` implies dispatchable subcommands, but the body has no `## up` / `## dispatch` / `## peek` / `## scale` / `## down` handlers — it is a linear narrative. An agent invoked as `/codex-lead dispatch "fix the parser"` has no defined branch to take. FIX: either add a short arg-routing table at the top mapping each hint verb to a section (up→§2+§3, dispatch→§4, peek→§5, scale→§7, down→§7), or shorten the hint to match reality.
- §6 (zai/GLM rotation) and §8 (~/.codex-team/<N> codex accounts) describe two DIFFERENT credential systems for the same `omx` engine and never reconcile. A lead who spawns via the §8 fast path is on codex pool N, so `maw zai status` reports on a pool their coders aren't using — they will monitor the wrong thing and miss the real rate limit. FIX: state explicitly which engine command routes to which pool. §8's engine line (`codex-setup.ts N`) = codex account N; §6's zai fill_first applies only when omx is pointed at the zai gateway.
- `maw team up`/`down`/`status`/`send` have NO usable --help — `maw team up --help` returns "team: unknown argument --help" and `maw team down --help` returns "unsafe team name '--help': leading dash rejected". Do not tell users to "check --help"; the flag lists must be inlined in the skill. Only `maw team load` and `maw team preflight` surface a usage string, and both take a FILE PATH, not a team name — mixing that up (`maw team preflight <team>`) errors with "unsafe path argument".
- §2's dry-run legend "live=skip, missing=would wake" omits the `dead` state. team_up_apply.rs:36 routes dead members to "resume in place" (a `maw wake ... --resume`), NOT a fresh wake — so a coder whose omx crashed comes back with its old session rather than re-ingesting the standing contract. That interacts badly with §3's "re-send the contract" step. FIX: document the third state and note `--force` (team_t5b_kill_window) as the way to force a truly fresh wake when you need the contract re-read.
- Do NOT 'fix' the §3 false-negative rule by adding a reflexive `maw send-enter` after every `maw hey`. SKILL.md:51-53 is right and the source backs it: `maw hey` already "write[s] receiver inbox and inject[s] into the target pane" per its own --help, so a blind extra Enter submits whatever the coder was mid-composing. Only send-enter after a `maw peek` visually confirms unsent text.
- Never substitute `maw team send` or SendMessage for `maw hey` when the target is an omx coder. Confirmed silent no-op: team_messaging.rs:103 only writes `<tool_dir>/inboxes/<agent>.json` and never touches tmux, so the coder never sees it and the lead sees a success exit code. This is the single highest-cost failure in the skill and the golden-rules block should stay at the very top.
- `maw wake --help` does NOT list `--session` or `--repo-path`, so a reviewer may 'correct' workaround #3 into something broken. Both flags are real and parsed (wake.rs:466 and wake.rs:472) — `team up` itself shells out with them (team_up_apply.rs:149-152). Leave that command exactly as written.

### SOURCE: lessons
FINDINGS:
- Provenance: both skills distill one real run — `codex-team-lessons/SKILL.md:8-9` states "Distilled from the maw-rs run (2026-07-05 → 07-12): 5 codex coders (peek/wave/tera/zai/more), ~35 PRs merged, 15-verb plugin extraction overnight, zero lead-written code." Files are single-file skills (no references/ subdir) at /Users/nat/.claude/skills/codex-team-lessons/SKILL.md (4.8K) and /Users/nat/.claude/skills/peek-and-nudge-every-10m/SKILL.md (3.0K).
- WHAT WENT WRONG #1 — shared identity: codex-team-lessons/SKILL.md:13-14 — "**1 coder : 1 worktree : 1 CODEX_HOME : 1 auth.json : 1 account.** Every violation of this caused an incident (token refresh collisions, 'refresh token already used')." Listed again as an anti-pattern at :82 "Two coders sharing a CODEX_HOME or account."
- WHAT WENT WRONG #2 — shared build queue: codex-team-lessons/SKILL.md:17-19 — "**Isolated build dirs always**: `CARGO_TARGET_DIR=/tmp/<repo>-target-<name>`. Never a machine-wide cargo queue — it deadlocks everyone. Only the package-cache lock is shared and cargo resolves it itself."
- WHAT WENT WRONG #3 — stale tokens after upgrade: codex-team-lessons/SKILL.md:20 — "After a codex CLI upgrade, restart every coder — upgrades reload stale on-disk tokens."
- WHAT WENT WRONG #4 — silent picker stalls: codex-team-lessons/SKILL.md:33-34 — "(Codex loves interactive pickers; a blocked picker silently wastes the slot until your next peek.)" Anti-pattern at :84 "Letting a picker sit — always answer or convert to ask-back."
- WHAT WENT WRONG #5 — near-miss merges on fail COUNTS not NAMES: codex-team-lessons/SKILL.md:52-54 — "**Read the FAIL NAMES before merging.** Counting fails is not enough — twice we nearly merged on '2 fails, probably the flakes.' Names ⊆ known-flaky list or no merge."
- WHAT WENT WRONG #6 — wrong base branch on an external PR: codex-team-lessons/SKILL.md:60-61 — "External PRs: **check the base branch before merging** (we squashed one into `main` because its base wasn't `alpha` — had to cherry-pick port it back)."
- WHAT WENT WRONG #7 — two coders independently widened a frozen contract: codex-team-lessons/SKILL.md:65-67 — "**Byte-frozen contracts + golden tests**: the parity harness caught two coders independently widening a frozen JSON contract. Machines catch what tired leads miss."
- WHAT WENT WRONG #8 — parallel rebase ping-pong: codex-team-lessons/SKILL.md:58-59 — "Serialize rebases when parallel PRs touch the same dispatch zone; otherwise they ping-pong CONFLICTING forever."
- DISPATCH CONTRACT (the 7 elements, codex-team-lessons/SKILL.md:22-37) — "A dispatch that produces a clean PR contains, every time: 1. **Issue ref + how to read it**: `gh issue view N` (write spec-kit issues: US/FR/AC/OQ). 2. **Branch name from origin/alpha**: `agents/<slug>-<issue>`. 3. **Hard constraints**: diff ≤250 lines (say 'split into PR-A/PR-B' up front), style gates. 4. **Exact gate commands** with the coder's own target dir. 5. **Done-report format**: PR link + exact gate evidence + root cause, to the lead pane. 6. **Ask-back rule** … 7. **Fresh context that changed**: e.g. 'flaky trio is FIXED — a red test is now real.'" Closing line :37 — "Vague dispatch = polite garbage back. DONE-CRITERIA teach the coder to own the loop."
- ASK-BACK WORDING THAT WORKED (verbatim, codex-team-lessons/SKILL.md:31-33) — "Never block on a picker. Questions → `maw hey <lead>` with QUESTION + options + your recommendation, then proceed on the recommendation or switch tasks." Longer dispatch-footer variant at peek-and-nudge-every-10m/SKILL.md:56-58 — "If you need a decision, don't block on a picker — maw hey <session>:1 with QUESTION + options + your recommendation, then proceed on your recommendation or switch to other work until the lead replies."
- NUDGE WORDING THAT WORKED (verbatim, peek-and-nudge-every-10m/SKILL.md:35) — `maw hey <session>:<p> "status? if blocked say what's blocking; if done send done-report to <session>:1"`
- CADENCE + ARMING (peek-and-nudge-every-10m/SKILL.md:18-23) — arm with **ScheduleWakeup** in dynamic /loop mode: `delaySeconds: 600`, `prompt: /peek-and-nudge-every-10m <session>` ("same input verbatim each turn"), `reason: "peek-and-nudge coder loop, 10m cadence"`. Explicit: "Do NOT create a cron job — ScheduleWakeup keeps it session-local and stoppable." Note the two skills disagree slightly on interval: peek skill says 10 min; codex-team-lessons/SKILL.md:41 says "Run a 10-minute peek loop" while the sibling /codex-lead skill description says "15-20min peek loop".
- FOUR-WAY CLASSIFICATION (peek-and-nudge-every-10m/SKILL.md:31-39) — **Working** (output scrolling / tool running) → leave alone. **Idle with task assigned** (bare `›` prompt, no done-report received) → send the status nudge. **Idle, no task** → "assign next item from the backlog (check open issues / lead's queue)." **Stuck** (defined as "same error visible two peeks in a row") → "intervene: teach, don't edit — send the command/approach, never write the code yourself."
- FINISHED path (peek-and-nudge-every-10m/SKILL.md:40-42) — "**Process done-reports** that arrived since last peek: gate the PR (full workspace, isolated `CARGO_TARGET_DIR`, check FAIL NAMES before merging), merge when green, close the issue by hand, deploy if user has standing authorization."
- MERGE GATING (codex-team-lessons/SKILL.md:49-56) — "Gate **full workspace** in a fresh clone/worktree, isolated target dir, `--no-fail-fast`, merged with **current** alpha (merge-preview), raw output to a log file." Plus: "Keep the known-flaky list written down, each entry proven 3/3 green standalone — then **pay down the flakes** (a dedicated stabilization PR paid for itself in one day of saved re-gates)." Merge authority rule at peek-and-nudge-every-10m/SKILL.md:64 — "Merge only with explicit or standing user authorization ('merge when gates confirm')."
- HOW THE LEAD AVOIDED BECOMING THE BOTTLENECK — four mechanisms: (a) TEACH-DONT-EDIT, codex-team-lessons/SKILL.md:45-46 "when a coder is stuck, send the command or approach. The moment the lead edits code, the lead becomes the bottleneck and the coder learns nothing"; (b) queue depth, :47 "Keep 1–2 tasks queued per coder tier; idle xhigh-effort coders are the biggest waste"; (c) instant picker resolution, :43-44 "Answer pickers immediately when you find them (`maw send-enter` on the right option) — then remind the ask-back rule"; (d) silence discipline, peek-and-nudge-every-10m/SKILL.md:43-44 "**Report** one short status line to the user only when something changed (merge, new dispatch, blocker). Silence on no-change peeks." Hard rule at :61 — "Lead dispatches only — NEVER writes code."
- SMOOTHNESS MULTIPLIERS (codex-team-lessons/SKILL.md:63-75) — build stamps ("binary embeds commit+buildtime; every deploy verified by `--version` flip … 'which build is this?' must be answerable in one command"); "**Pilot before batch**: one verb extracted end-to-end (with rollback drill) before batching 14 more"; "**Write-back immediately**: guides (`docs/agent-guides/*`), AGENTS.md rules, and memory files written the same day. The next coder reads the scar instead of repeating it"; "**Report hygiene**: coder done-reports carry root cause, not just 'done'. Lead relays outcome + evidence to the human, including own mistakes plainly."
- THE 7 ANTI-PATTERNS, verbatim (codex-team-lessons/SKILL.md:77-86) — "Lead 'just quickly fixing' code — breaks the whole delegation model. / Merging on fail counts without names. / Machine-wide cargo/build queues. / Two coders sharing a CODEX_HOME or account. / Dispatch without diff budget → 800-line PRs nobody can gate. / Letting a picker sit — always answer or convert to ask-back. / Raw tmux instead of maw verbs — bypasses logging and safety hooks."
- Verb inventory verified against the installed binary (`command maw help --all`): `maw peek` = "Peek at recent agent output without attaching; use capture when you need full scrollback"; `maw capture` = full scrollback; `maw hey` = "Send a signed message to another oracle over federation"; `maw send` = raw text without Enter; `maw send-enter` = press Enter (the picker-answering primitive); `maw team`/`maw t`, `maw squad`, `maw gather`/`maw scatter`, `maw activity` ("Classify pane activity by diffing peek snapshots") also exist — `maw activity` is a plausible mechanical aid for the working/idle classification step the peek skill does by eyeball.
VERIFIED COMMANDS:
- maw ls | rg -i "$(basename "$(git rev-parse --show-toplevel)")"
- for p in 2 3 4 5 6; do echo "── pane $p:"; maw peek <session>:$p | tail -4; done
- maw peek <session>:<pane>
- maw hey <session>:<pane> "status? if blocked say what's blocking; if done send done-report to <session>:1"
- maw send-enter <target>
- maw work . --wt mawrs-codex-<name> -e omx-N
- maw help --all
- maw activity
- maw capture <target>
- gh issue view N
- gh pr create --base alpha --head agents/<slug>-<issue> --title "..."
- CARGO_TARGET_DIR=/tmp/<repo>-target-<name> cargo test --workspace --no-fail-fast
PITFALLS:
- Two coders sharing a CODEX_HOME/auth.json/account → "refresh token already used" token-refresh collisions. Fix: enforce 1 coder : 1 worktree : 1 CODEX_HOME : 1 auth.json : 1 account, auth by symlink `agents/<wt>/.codex/auth.json → ~/.codex-team/N` (codex-team-lessons/SKILL.md:13-16).
- A machine-wide cargo target dir deadlocks the entire team. Fix: per-coder `CARGO_TARGET_DIR=/tmp/<repo>-target-<name>`; only the package-cache lock is shared and cargo handles that itself (:17-19).
- A codex CLI upgrade leaves coders on stale on-disk tokens. Fix: restart every coder after any codex upgrade (:20).
- An interactive picker silently burns a coder's slot until the next peek — invisible in throughput numbers. Fix: answer it on sight with `maw send-enter`, then re-send the ask-back rule so the coder converts future decisions into `maw hey` QUESTION + options + recommendation and keeps working (:33-34, :43-44).
- Merging on fail COUNTS ("2 fails, probably the flakes") nearly shipped real regressions twice. Fix: read the FAIL NAMES; merge only if names ⊆ a written known-flaky list where each entry was proven 3/3 green standalone — then spend a stabilization PR to delete the flakes (:52-56).
- Gating the PR branch alone, or against stale alpha, hides conflicts. Fix: gate the FULL workspace in a fresh clone/worktree, isolated target dir, `--no-fail-fast`, merged against CURRENT alpha (merge-preview), raw output to a log file (:50-51).
- An external/contributor PR whose base wasn't `alpha` got squashed into `main` and needed a cherry-pick port back. Fix: check the base branch on every non-team PR before merging (:60-61).
- Parallel PRs touching the same dispatch zone ping-pong CONFLICTING forever. Fix: serialize the rebases (:58-59).
- Dispatch without a diff budget produces 800-line PRs nobody can gate. Fix: state "diff ≤250 lines" AND pre-declare the split ("split into PR-A/PR-B") in the dispatch itself, before the coder starts (:28, :83).
- Vague dispatch returns "polite garbage." Fix: every dispatch carries all 7 elements — issue ref + how to read it, branch name off origin/alpha, diff budget + style gates, the exact gate commands with the coder's own target dir, the done-report format (PR link + gate evidence + root cause), the ask-back rule, and any context that CHANGED since last dispatch (e.g. "flaky trio is FIXED — a red test is now real") (:24-37).
- The lead "just quickly fixing" one thing collapses the delegation model and teaches the coder nothing. Fix: TEACH-DONT-EDIT — send the command or approach into the pane, never the code (:45-46, :79).
- Idle high-effort coders are the single biggest waste, and idleness is invisible without a loop. Fix: keep 1-2 tasks queued per coder tier and run the 10-minute peek loop; classify bare `›` prompt as idle, and "same error two peeks in a row" as stuck (:47, peek skill :31-39).
- Raw tmux send-keys bypasses maw's logging and safety hooks. Fix: always use maw verbs (peek/hey/send-enter/capture) (:85, peek skill :27, :62).
- Cron for the lead loop leaves an unstoppable job behind. Fix: use ScheduleWakeup with `delaySeconds: 600` and the same verbatim prompt each turn; re-arm every wakeup even when only a gate is running; stop with `stop: true` when the user says stop or all coders close (peek skill :18-23, :46-51).
- Chatty peek reports drown the human in no-op status. Fix: report one line only when something changed — merge, new dispatch, blocker — and stay silent otherwise (peek skill :43-44).
- Tired leads miss contract drift; two coders independently widened a frozen JSON contract. Fix: byte-frozen contracts + golden/parity tests, plus build stamps (commit+buildtime embedded, every deploy verified by a `--version` flip) so "which build is this?" is one command (:65-70).
- Batching a repetitive extraction before proving it end-to-end multiplies a single design error 15×. Fix: pilot one unit end-to-end WITH a rollback drill, then batch the rest (:71-72).

### SOURCE: charter-and-live
FINDINGS:
- CHARTER FILE: /opt/Code/github.com/Soul-Brews-Studio/maw-rs/ψ/teams/maw-rs-team.yaml — 142 lines, 7.2K. Top-level keys only: `name`, `project`, `session`, `goal`, `engines`, `members`, `lifecycle`. Everything else in the file is comments (spawn recipe, role→crate map, key lessons).
- CHARTER FIELD `name: maw-rs-team` — the team id passed to `maw team up/down/status <name>`. It is ALSO the key under which maw registers the team in the tool store at /Users/nat/.claude/teams/maw-rs-team/config.json (verified: that file exists and lists 6 members lead/cli/engine/auth/plugin/infra + createdAt 1784345319472).
- CHARTER FIELD `project: Soul-Brews-Studio/maw-rs` — org/repo the team operates on; resolves the repo root that worktree paths are relative to.
- CHARTER FIELD `session: 33-maw-rs` — the tmux session name coder windows get created in. Verified live: tmux session `33-maw-rs` exists (created Fri Jul 24 00:00:45) but has exactly 1 window `maw-rs*` (1 pane, the Claude lead at /opt/Code/github.com/Soul-Brews-Studio/maw-rs).
- CHARTER FIELD `goal:` (block scalar) — the shared contract text injected into every coder: gate command (`cargo build + cargo test + cargo clippy -- -D warnings`), PR base rule (alpha never main), file-size cap (≤250 lines), and the report contract `ACK (starting) → blocked → done(PR#, all gates green)`.
- CHARTER FIELD `engines:` — a named map omx-1..omx-6, each value a full SHELL STRING (not a binary name). Each is `bun $HOME/.claude/skills/oracle-team/scripts/codex-setup.ts <N> && CODEX_HOME=$PWD/.codex OMX_AUTO_UPDATE=0 omx --direct --madmax`. The `&&` chaining is load-bearing: codex-setup.ts prepares the home then EXITS, so omx runs in the pane shell and keeps its TTY (script header at ~/.claude/skills/oracle-team/scripts/codex-setup.ts:3-5 says a forked omx loses the terminal → exit 1).
- CHARTER FIELD `members:` — list of role objects. Each has: `role` (tmux window name), `name` (agent identity + worktree dir name + branch name), `engine` (either the literal `claude` for the lead or an `engines:` key), `worktree` (literal `false` for the lead, else an EXPLICIT RELATIVE PATH like `agents/mawrs-codex-cli`), `branch` (git branch, `alpha` for lead), and `prompt` (block scalar = the coder's standing orders: scope globs, WAIT-for-maw-hey, own branch/worktree, gate command, PR base, report line `maw hey 33-maw-rs:maw-rs "ACK/blocked/done — …"`, signature `[33-maw-rs:<name>]`, and 'Never touch other worktrees').
- CHARTER: engine→pool assignment is NOT sequential — cli=omx-1, engine=omx-2, auth=omx-6, plugin=omx-4, infra=omx-5. omx-3 is unused by this charter. Verified on disk: agents/mawrs-codex-cli/.codex/auth.json → /Users/nat/.codex-team/1/auth.json; engine → /2; infra → /5; auth → /6. Exactly matches the charter.
- CHARTER FIELD `lifecycle: {worktree: true, merge_on_shutdown: false}` — worktree lifecycle is managed by maw; shutdown does NOT auto-merge coder branches (lead merges one PR at a time, per the lead prompt).
- CHARTER comment block lines 135-141 'Key lessons' encodes the real traps: `maw run` for engine launch vs `maw hey` for task dispatch (hey prepends `[node:oracle]` → zsh glob error); explicit worktree path per member because `worktree: true` produced a literal "true" directory; OMX_AUTO_UPDATE=0 inline not in config.toml; separate CODEX_HOME per coder; Cargo.lock through lead only; teardown = tmux kill-window + mv /tmp + git worktree prune, never rm -rf.
- LIVE FLEET STATE: `maw ls --verbose` returned 19 sessions, each with exactly ONE pane (verified independently by `tmux list-panes -a`). There are ZERO codex coder panes alive anywhere on this machine right now. `maw more status` in the maw-rs repo prints `live coders: 0` / `(no codex coder windows)`. `maw team ls` shows maw-rs-team = 6 members, STATUS `no live panes`, 0 zombies.
- NAME CORRECTION: there are no maw-rs coders called wave/peek/zai/more. Those are maw SUBCOMMANDS, verified by --help: `maw wave <start|status|dispatch|teardown>`, `maw peek <tmux-target> [--lines N] [--history]`, `maw zai <status|mon|test> [--fleet <group>]` (Z.AI token pool), `maw more codex [N] [--session <s>] [--dry-run] [-e|--engine <e>] [--pool <n|engine>]`. The real maw-rs coder names are mawrs-codex-{cli,engine,auth,plugin,infra}.
- ON-DISK LAYOUT (maw-rs, the WORKING pattern): /opt/Code/github.com/Soul-Brews-Studio/maw-rs/agents/ contains 4 dirs — mawrs-codex-auth, mawrs-codex-cli, mawrs-codex-engine, mawrs-codex-infra. `git worktree list` confirms all four are real worktrees, each on its own branch agents/mawrs-codex-<role>, all four parked at the SAME commit 9007f783.
- MISSING COACH: the charter defines 5 coders but `mawrs-codex-plugin` (engine omx-4, branch agents/mawrs-codex-plugin) has NO worktree on disk and no branch. Only 4 of 5 charter coders materialized.
- PER-CODER CODEX HOME IS `.codex`, NOT `.codex-home`. Each worktree contains <worktree>/.codex/ — e.g. /opt/Code/github.com/Soul-Brews-Studio/maw-rs/agents/mawrs-codex-cli/.codex/. There is no `.codex-home` anywhere in the tree.
- WHAT'S INSIDE A CODER'S .codex (verified, mawrs-codex-cli): SYMLINKS out to the shared pool — AGENTS.md → ~/.codex-team/1/AGENTS.md, agents → ~/.codex-team/1/agents, auth.json → ~/.codex-team/1/auth.json, prompts → ~/.codex-team/1/prompts, skills → ~/.codex-team/1/skills. LOCAL REAL FILES — config.toml (12.3K), hooks.json (2.3K), installation_id, .personality_migration, history.jsonl, and the per-coder SQLite set: goals_1.sqlite, logs_2.sqlite (+ -shm/-wal), memories_1.sqlite, state_5.sqlite, models_cache.json. Local dirs: cache/, plugins/, sessions/, shell_snapshots/, tmp/. Sizes 43-53M each.
- WHY THE SPLIT: codex-setup.ts header (~/.claude/skills/oracle-team/scripts/codex-setup.ts:6-12) states the design — auth.json symlinked to reuse the credential; agents/AGENTS.md/skills/prompts symlinked as read-only config; hooks.json copied locally AND trusted for that exact local path; config.toml copied with reasoning_effort forced to xhigh; 'own SQLite/locks created fresh by omx → no fleet-wide collision'.
- CONFIG PROOF: mawrs-codex-cli/.codex/config.toml line 3 `model_reasoning_effort = "xhigh"`, line 6 `model = "gpt-5.5"`, line 8 `model_context_window = 250000`, line 9 `model_auto_compact_token_limit = 200000`; carries a `[projects."/opt/Code/github.com/Soul-Brews-Studio/maw-rs"] trust_level = "trusted"` block plus an appended section delimited by `# OMX worktree-local Codex hook trust state (managed by codex-setup.ts)` (line 258) … `# End OMX worktree-local Codex hook trust state` (line 282) holding per-event hash trust entries (session_start, pre/post_tool_use, pre/post_compact, stop, user_prompt_submit).
- DRIFT / STALE COACH: mawrs-codex-engine/.codex is set up by an OLDER codex-setup generation — it has only `auth.json` and `skills` symlinks (no AGENTS.md, no agents, no prompts), and its config.toml is 5.6K/146 lines with the hook-trust block at lines 123-144, versus cli's 12.3K/282+ lines. All four .codex dirs last changed Jul 24 ~11:00, each holding exactly 1 session jsonl.
- SHARED POOL LAYOUT: ~/.codex-team/{1,2,3,4,5,6} plus ~/.codex-team/hermes. Pool 1 holds the real originals — AGENTS.md 20.3K, auth.json 4.2K, config.toml 10.5K, hooks.json 3.3K, history.jsonl 214.5K, logs_2.sqlite 80.6M, state_5.sqlite 572K, plus agents/ prompts/ skills/ sessions/ plugins/ .omx/. Sibling dirs also present: ~/.codex (personal), ~/.codex-crew-master, ~/.codex-maw-rs, ~/.codex-team-removed.
- KRU32 TEAM — charter at /opt/Code/github.com/the-oracle-keeps-the-human-human/kru32-oracle/ψ/teams/kru32-team.yaml (1.5K). Same schema but SIMPLER: `session: 81-kru32`, engines omx-1/3/4/5 written as plain inline strings `OMX_AUTO_UPDATE=0 CODEX_HOME=~/.codex-team/<N> omx --direct --madmax` — NO codex-setup.ts, NO $PWD/.codex, no `goal:`, no per-member `prompt:`, no `lifecycle:`. 7 members: lead=kru32-oracle (claude, worktree:false), assistant (omx-1, worktree:false, shares lead cwd), compile (omx-3), coder-1 (omx-1), coder-2 (omx-3), qa (omx-4), designer (omx-5). Note omx-1 and omx-3 are each DOUBLE-BOOKED.
- KRU32 uses `worktree: true` (bare boolean) + a separate `branch: agents/kru32-<name>` field, which is exactly the form the maw-rs charter's comments warn about (literal "true" dir bug); maw-rs upgraded to explicit `worktree: agents/<name>` paths.
- KRU32 ON DISK: session 81-kru32 does NOT exist (`tmux list-windows -t 81-kru32` → "can't find session"), and kru32-team is not registered in ~/.claude/teams at all. agents/ has 10 worktrees — 5 from the charter (coder-1, coder-2, compile, qa, designer, all detached HEAD) plus 5 added ad hoc beyond the charter: kru32-bins-flash, kru32-copy, kru32-petpacks-flash, kru32-wasm-compile, kru32-wasm-preview (these 5 ARE on named agents/* branches).
- KRU32 HAS NO PER-WORKTREE CODEX HOME: checked all 10 agents/*/ — every one reports `.codex NO` and `.codex-home NO`. Its coders point CODEX_HOME straight at the shared ~/.codex-team/N, which is precisely the sqlite-lock collision codex-setup.ts was written to eliminate. maw-rs is the evolved pattern; kru32 is the earlier one.
- KRU32 wraps the maw team verbs in a Makefile: Makefile:4 `TEAM = kru32-team`, then targets team-up (`maw team up $(TEAM)`), team-down (`maw team down $(TEAM)`), team-status (`maw team status $(TEAM)`), team-peek. Archived charter at ψ/teams/archive/kru32-wasm-team.yaml (8.8K) + a .STATUS.md.
- ARCHIVED maw-rs CHARTERS at ψ/teams/archive/: team-codex3.yaml, team-codex5.yaml, team-omx5.yaml, each with a *-retired-20260709.yaml twin — evidence the team shape was iterated (3 coders → 5 coders → omx5 → the current role-based 5).
- maw-rs agents/ has git history of teardown: `git log -- agents/` shows `21237348 chore: untrack accidental agents/ junk files` and three `wip: auto-save before team-down` commits — i.e. team-down auto-commits coder WIP before killing windows.
VERIFIED COMMANDS:
- maw ls --verbose
- maw team ls
- maw team status
- maw team status <team-name>
- maw more status
- maw more codex [N] [--session <s>] [--dry-run] [-e|--engine <e>] [--pool <n|engine>]
- maw peek <tmux-target> [--lines N] [--history]
- maw wave <start|status|dispatch|teardown>
- maw zai <status|mon|test> [--fleet <group>]
- maw team up <team-name>
- maw team down <team-name>
- tmux list-sessions
- tmux list-windows -t 33-maw-rs
- tmux list-panes -a -F '#{session_name}:#{window_index}.#{pane_index} #{window_name} | #{pane_current_path} | #{pane_current_command}'
- git -C /opt/Code/github.com/Soul-Brews-Studio/maw-rs worktree list
- git -C <repo> for-each-ref --format='%(refname:short) | %(committerdate:relative)' 'refs/heads/agents/*'
- bun $HOME/.claude/skills/oracle-team/scripts/codex-setup.ts <pool-index>
- make team-up   # kru32-oracle only; wraps maw team up kru32-team
PITFALLS:
- Do NOT read coder names off the parent's prompt — wave/peek/zai/more are maw SUBCOMMANDS, not agents. `maw wave --help`, `maw peek --help`, `maw zai --help`, `maw more --help` all confirm. The real maw-rs coders are mawrs-codex-{cli,engine,auth,plugin,infra}.
- The per-coder Codex home is `<worktree>/.codex`, NOT `.codex-home`. Globbing for `agents/*/.codex-home` returns nothing and will make you wrongly conclude coders share a home. Check `agents/*/.codex` and then `readlink agents/*/.codex/auth.json` to see which ~/.codex-team/N pool it borrows.
- `maw ls --verbose` lists sessions and their panes, but a team that is DOWN still leaves its worktrees and .codex dirs on disk. Presence of agents/<name>/.codex proves the team RAN, not that it is live. Cross-check with `maw more status` (`live coders: 0`) and `tmux list-panes -a` before claiming anything is running.
- `maw team status` with no argument dumps EVERY registered team (30+) and exits 1 outside a team repo. Always pass the team name: `maw team status maw-rs-team`.
- zsh glob expansion bites git refspecs: `git for-each-ref refs/heads/agents/mawrs-codex-*` fails with `(eval):1: no matches found`. Quote the pattern — `'refs/heads/agents/*'`. Same for `ls agents/*.json` when no match exists; use `2>/dev/null` or quote.
- The charter is aspirational, the disk is truth. maw-rs-team.yaml defines 5 coders; only 4 worktrees exist (mawrs-codex-plugin is missing entirely — no dir, no branch). Always diff `members:` against `ls agents/` and `git worktree list`.
- Coder .codex dirs drift between codex-setup.ts generations. mawrs-codex-engine has only auth.json+skills symlinks and a 146-line config.toml, while mawrs-codex-cli has 5 symlinks and a 282+-line config. If a coder misbehaves after respawn, re-run `bun ~/.claude/skills/oracle-team/scripts/codex-setup.ts <N>` from inside that worktree to regenerate the home rather than hand-editing.
- Never fork omx from the setup script. The engine string MUST be `codex-setup.ts <N> && CODEX_HOME=$PWD/.codex … omx …` — the `&&` keeps omx in the pane's own shell. A forked omx loses its TTY and exits 1 (documented at codex-setup.ts:3-5).
- Two coders sharing one CODEX_HOME is the kru32 failure mode: kru32-team.yaml points omx-1 at BOTH `assistant` and `coder-1`, and omx-3 at both `compile` and `coder-2`, all against the raw ~/.codex-team/N — with no worktree-local .codex the SQLite (logs_2/state_5/memories_1) locks collide fleet-wide. Copy the maw-rs pattern (worktree-local .codex + symlinked auth) instead.
- `worktree: true` as a bare boolean is a known bug source (creates a literal directory named "true"). maw-rs-team.yaml fixes it by giving each member an explicit relative path (`worktree: agents/mawrs-codex-cli`); kru32-team.yaml still uses the bare boolean form.
- Launch and dispatch are different verbs: use `maw run <session>:<role> '<engine cmd>'` to boot omx, then `maw peek` to verify the boot, and only then `maw hey` for tasks. `maw hey` prepends a `[node:oracle]` signature that a raw shell interprets as a zsh glob and errors out.
- Teardown is `tmux kill-window` + `mv agents/<name> /tmp/` + `git worktree prune` — never `rm -rf`. maw-rs's `git log -- agents/` shows `wip: auto-save before team-down` commits, so a careless rm destroys uncommitted coder work that team-down would otherwise preserve.

### SOURCE: maw-verbs
FINDINGS:
- BINARY: `maw` on PATH is a zsh FUNCTION (wraps `command maw`, adds only the `maw warp` verb). Real binary = /Users/nat/.local/bin/maw, 37.3M. `maw --version` → `maw-rs v26.7.25-alpha.1755 (fd82a9b) built 2026-07-25 17:55:25 +0700`. Source repo /opt/Code/github.com/Soul-Brews-Studio/maw-rs is at HEAD 3979e884 which does NOT contain fd82a9b — the checkout is a different lineage than the installed build, so trust the binary/plugin-dir, not the repo checkout.
- VERB SURFACE: `maw --help` shows only ~22 core verbs. `maw help --all` reports **registered commands (197)** in 4 tiers: core (40), standard (80), extra (41), other (36). Everything the team-spawning skill needs is registered; several are simply absent from the curated menu.
- HIDDEN-BUT-WORKING (not in `maw --help`, present in `maw help --all`): sleep, done, finish, take, swarm, new, pane, panes, peek, capture, join, gather, scatter, promote, worktree, workon, team, t, squad, tile, split, view, zoom, activity, inspect, whoami, tag, wave, oracle-workon.
- `maw work --help` → `usage: maw work <repo|.|path|url> [task] [--wt [slug]] [--fresh] [--name <stable>] [-e <engine>] [--layout nested|legacy]`. `maw workon --help` prints the IDENTICAL usage string — they are the same surface. Neither supports --dry-run (verified: `maw work . --dry-run` printed the workon usage and spawned nothing).
- `maw swarm --help` → `usage: maw swarm [agents...] [--tiled] [--count N] [--parent-session-id <id>] [--session-id <id>]`. Examples: `maw swarm` = 3 claude agents (default); `maw swarm claude codex opencode`; `maw swarm codex codex codex`; `maw swarm --count 5`; `maw swarm --tiled` = equal layout. Supported agents: claude, codex, opencode, aider, or any command. **swarm has NO --wt and no worktree support at all** — all panes land in the current cwd.
- `maw kill` → `usage: maw kill <target>[:window] [--pane N] [--index N|--all] [--force] [--peer <alias>]`. **`--pane N` is genuinely parsed, not just documented**: `maw kill zzz-nonexistent-probe --pane abc` → `--pane must be a non-negative integer (got abc)`, while `maw kill zzz-nonexistent-probe --pane 3` reached target resolution and failed with `✗ session 'zzz-nonexistent-probe' not found`. Control: `maw kill x --bogus-flag` → `"--bogus-flag" looks like a flag, not a target.`
- `maw sleep --help` → `usage: maw sleep <oracle> [window] | maw sleep --all-done [--dry-run]`. Verified read-only: `maw sleep --all-done --dry-run` → `sleep --all-done 71-arra-oracle-skills-cli (dry-run) / ! no done panes found / No changes made`.
- `maw done --help` → `usage: maw done <window-name> [--force] [--dry-run] [--clean-branch] [--worktree <path>] or maw done --all [<oracle>] [--force] [--dry-run] [--clean-branch]`. Does retro-save + kill window + REMOVE the worktree. `maw finish` is an alias. Verified `maw done zzz-nonexistent-probe --dry-run` → `no done target matched 'zzz-nonexistent-probe'`.
- `maw hey --help` → `usage: maw hey <target> <message> [--inbox] [--force deprecated] [--approve] [--trust]`, plus `-f <file>` and `-` (stdin) forms. Documented target forms: `<oracle-window>` (local-only bare name), `local:<agent>`, `<session>:<window>[.<pane>]`, `<node>:<session>`, `<node>:<session>:<window>`.
- **`maw hey` DOES support `--dry-run`, but it is MISSING from `maw hey --help`.** Proven two ways: (a) `strings /Users/nat/.local/bin/maw` yields the send_dry_run_output literals `dry-run: `, ` -> local `, ` -> peer ` at offsets 13878-13883; (b) live execution below. Source confirms at maw-rs crates/maw-cli/src/core_impl/send_federation.rs:452 (`"--dry-run" => dry_run = true`), :123-124, :811 (`fn send_dry_run_output`), and the source's own usage string at :599 already lists `[--dry-run]` — the installed binary's help text is simply stale.
- **`maw hey` DOES accept a dotted PANE target.** Live verified with --dry-run (no message sent): `maw hey 71-arra-oracle-skills-cli:1.0 "PROBE" --dry-run` → `dry-run: hey 71-arra-oracle-skills-cli:1.0 -> local 71-arra-oracle-skills-cli:1.0`; `maw hey 33-maw-rs:1.0 "PROBE" --dry-run` → `dry-run: hey 33-maw-rs:1.0 -> local 33-maw-rs:1.0`. Other forms resolve too: `33-maw-rs:1` → `-> local 33-maw-rs:1`; bare `maw-rs` → `-> local 33-maw-rs:1`; `local:maw-rs` → `-> self-node 33-maw-rs:1`.
- `maw peek --help` → `usage: maw peek <tmux-target> [--lines N] [--history]` and `maw peek [--lines N]` (no target = fleet-wide one-line-per-oracle summary). **peek accepts dotted pane targets** — verified `maw peek 71-arra-oracle-skills-cli:1.0 --lines 3` and `maw peek 33-maw-rs:1.0 --history --lines 2` both returned pane content. It also accepts raw tmux pane ids (`maw peek %3001`) and bare oracle names (`maw peek maw-rs`).
- peek/hey dotted targets are passed straight through to tmux, so the indices must actually exist. `maw peek 71-arra-oracle-skills-cli:1.1` → `peek capture failed ...: tmux exited with status 1: can't find pane: 1`. On this fleet every session has exactly ONE window at index **1** with ONE pane at index **0** (`tmux list-panes -a` → `33-maw-rs:1.0`, `60-digger:1.0`, ...), so the canonical dotted form here is `<session>:1.0`, NOT `<session>:1.1`.
- **`maw tile --wt` does NOT create a git worktree — it does not even create a nested dir. It is a cosmetic label ONLY.** The shipped plugin is WASM (/Users/nat/.maw/plugins/tile/plugin.wasm, entry kind `wasm`, export `handle`), built from /Users/nat/.maw/plugins/tile/src/lib.rs. In that Rust source `--wt` is parsed at lib.rs:197-214 into a `Wt` enum, and the ONLY non-test consumer is `fn summary()` at lib.rs:682-687, which merely appends `worktree` / `worktree:<name>` to the printed flag list. `fn spawn()` (lib.rs:217-277) derives pane cwd purely from `resolve_path(opts.path, context)` — `opts.wt` is never consulted. Net effect: `maw tile 3 --wt feat` prints `✓ 3 panes tiled (worktree:feat)` while all 3 panes sit in the current cwd on the current branch.
- Corroborating evidence that tile CANNOT create a worktree: plugin.json declares only tmux capabilities (`tmux:read`, `tmux:raw:list-panes|split-window|select-pane|set-option|send-keys|select-layout|swap-pane|kill-pane`) — no exec/git capability. The wasm's single host import is `maw_tmux_command` (lib.rs:26). `strings plugin.wasm | rg 'worktree add|worktree list|worktree remove|\.wt-|checkout -b|rev-parse'` → ZERO matches.
- TRAP — stale TS decoy: /Users/nat/.maw/plugins/tile/impl.ts and index.ts are leftover maw-js code that DOES implement real worktrees (impl.ts:219-243 calls `createWorktree`, :362 `git worktree list --porcelain`, :379 `git worktree remove --force`). These files are NOT what runs. Reading them will make you wrongly conclude `--wt` works. The wasm is authoritative.
- TRAP — stale tile help: `maw tile --help` / `maw tile -h` prints plugin.json's `cli.help` field: `maw tile [N] [--border] [--path <dir>] [--cmd <cmd>] [--engine <name>] | clean | swap <a> <b>` — which omits --wt, --layout, --shell entirely. The wasm's own internal USAGE (lib.rs:9, also visible via `strings plugin.wasm`) is the real one: `maw tile [N] [--border] [--wt <name>] [--layout nested|legacy] [--path <dir>] [--cmd <cmd>] [--shell] [--engine <name>] [--parent-session-id <id>] [--session-id <id>]`. Also `maw tile help` is NOT a subcommand → `tile: expected a number, got 'help'`.
- WHAT ACTUALLY CREATES REAL GIT WORKTREES: (1) `maw work|workon <repo> [task] --wt [slug] [--layout nested|legacy]`; (2) `maw worktree add <name> [--base <ref>]`; (3) `maw team spawn <team> <role> ... --worktree <path>` (explicit path, caller-supplied). Confirmed real via `git worktree list` on this repo: `/opt/.../arra-oracle-skills-cli/agents/codex-buddy  232f3c9 [agents/codex-buddy]` (nested layout) and `/opt/.../arra-oracle-skills-cli-2675-phase1` (legacy sibling layout).
- `maw worktree --help` → `usage: maw worktree <ls|clean> [--dry-run] or maw worktree add <name> [--base <ref>]`. `maw worktree ls` is a great read-only team-health probe — emits TSV `path\tbranch\tmerged?\tdirty?\tlive?` where `live?` resolves to the tmux target, e.g. `yes:71-arra-oracle-skills-cli:arra-oracle-skills-cli`.
- TEAM VERBS (relevant to a team-spawning skill): `maw team <create|new|list|ls|status|tasks|oracle-members|members|lives|history|plan|preflight|check|load|spawn|spawn-from|send|msg|broadcast|inbox|invite|up|bring|apply|reassign|liveness|down|remove|delete|rm|prune|gc|shutdown|resume|enter|send-enter|add|task|done|assign>`. `maw t` is an alias.
- team sub-usages verified by invoking with no args (safe, prints usage): `maw team up <team> [--session <name>] [--members <roles>] [--only <a,b>] [--dry-run] [--status] [-e <engine>]`; `maw team spawn <team> <role> [--engine <engine>] [--model <model>] [--cwd <path>] [--worktree <path>] [--prompt <text>] [--parent-session-id <id>] [--session-id <id>] [--exec]`. From binary strings: `maw team down <team> [--all] [--keep <a,b>] [--dry-run] [--status]`, `maw team bring <team> [--session <session>] [--split] [--gather] [--dry-run] [-e <engine>]`, `maw team remove <member> [--keep-branch] [--dry-run]`, `maw team gc [--confirm] [--age 7d] [--keep-empty]`, `maw team spawn-from <team.yaml|team.json> [--approve] [--exec]`, `maw team load <team.yaml|team.json> --no-spawn`, `maw team shutdown <name> [--force] [--merge]`, `maw team enter <agent|all>`, `maw team assign <task-id> <agent> [--team <name>]`, `maw team done <task-id> [--team <name>]`, `maw team broadcast <team> <message>`.
- `maw team --help` and `maw team up --help` do NOT work — `--help` is rejected as `team: unknown argument --help`. The way to get a team sub-usage is to run the subcommand with NO arguments (it prints usage and exits without side effects). Verified for `up`, `spawn`.
- `maw squad --help` emits `⚠ [dev-tier: bun] squad — TS runs unsandboxed; ship tier = WASM (maw plugin build)` and no usage. squad is a lead-centric wrapper (start/join/say/list) still on the dev TS tier — do NOT depend on it in a shipped skill.
- Useful read-only companions confirmed working: `maw ls -v` (TARGET column prints `<window>.<pane>` form e.g. `maw-rs.0`, plus AGE/ANNOTATION/TITLE), `maw whoami` (prints session / window(@id) / pane(%id) / exact target), `maw peek` with no target (fleet summary), `maw worktree ls`, `maw team list`, `maw team status`, `maw inspect`, `maw capture`, `maw locate`, `maw activity`.
VERIFIED COMMANDS:
- maw --version
- maw --help
- maw help --all
- maw work --help
- maw workon --help
- maw tile --help
- maw swarm --help
- maw kill
- maw sleep --help
- maw done --help
- maw hey --help
- maw peek --help
- maw worktree --help
- maw worktree ls
- maw team
- maw team up
- maw team spawn
- maw team list
- maw team status
- maw ls -v
- maw whoami
- maw peek <session>:1.0 --lines 20
- maw peek <session>:1.0 --history --lines 50
- maw peek %<pane-id> --lines 20
- maw peek <oracle-name> --lines 20
- maw peek --lines 2
- maw hey <session>:1.0 "message" --dry-run
- maw hey <oracle> "message" --dry-run
- maw hey local:<agent> "message" --dry-run
- maw hey <node>:<session>:<window> "message" --dry-run
- maw hey <target> -f <file>
- maw hey <target> --inbox "message"
- maw kill <session>:<window> --pane 2
- maw kill <target> --all
- maw kill <target> --force
- maw sleep <oracle> [window]
- maw sleep --all-done --dry-run
- maw done <window-name> --dry-run
- maw done <window-name> --clean-branch
- maw done --all <oracle> --dry-run
- maw work <repo|.|path|url> [task] --wt <slug> --layout nested -e claude
- maw workon <repo> [task] --wt <slug> --fresh --name <stable>
- maw worktree add <name> --base <ref>
- maw worktree clean --dry-run
- maw swarm codex codex codex --tiled
- maw swarm --count 5
- maw tile 3 -e claude
- maw tile 3 --path /abs/worktree/path
- maw tile 3 --path /abs/path --cmd "bun test"
- maw tile clean
- maw tile swap 1 2
- maw team up <team> --dry-run
- maw team up <team> --session <name> --only a,b -e codex
- maw team spawn <team> <role> --engine codex --worktree /abs/path --prompt "..."
- maw team down <team> --dry-run
- maw team bring <team> --split --gather --dry-run
- maw team broadcast <team> "message"
- maw team assign <task-id> <agent> --team <name>
- maw team done <task-id> --team <name>
- git worktree list
- tmux list-panes -a -F '#{session_name}:#{window_index}.#{pane_index} #{pane_id}'
- strings /Users/nat/.local/bin/maw | rg 'usage: maw team'
PITFALLS:
- `maw tile --wt <name>` SILENTLY LIES. It prints `✓ 3 panes tiled (worktree:feat)` but creates no git worktree and no directory — every pane stays in the current cwd on the current branch, so N agents will stomp each other's edits. Proof: /Users/nat/.maw/plugins/tile/src/lib.rs:684-687 uses `opts.wt` only inside `fn summary()`; `fn spawn()` at :217-277 sets cwd from `resolve_path(opts.path, ...)` alone. FIX: never use `tile --wt` for isolation. Create the worktrees first with `maw work <repo> --wt <slug>` or `maw worktree add <name>`, then place panes with `maw tile N --path /abs/worktree/path` (or use `maw team spawn --worktree <path>`). Verify with `git worktree list` before dispatching any work.
- Do not read /Users/nat/.maw/plugins/tile/impl.ts or index.ts to learn tile's behavior — they are dead maw-js leftovers that DO implement real worktrees (`createWorktree`, `git worktree list --porcelain`) and will lead you to the exact wrong conclusion. The shipped artifact is plugin.wasm (plugin.json `entry.kind: wasm`), built from src/lib.rs. FIX: read src/lib.rs, or `strings plugin.wasm`.
- `maw tile --help` is stale — it prints plugin.json's `cli.help` string, which omits `--wt`, `--layout`, and `--shell`. Conversely `maw tile help` errors with `tile: expected a number, got 'help'`. FIX: get the true flag list from `strings /Users/nat/.maw/plugins/tile/plugin.wasm | rg -- '--'` or lib.rs:9.
- `maw hey --help` omits `--dry-run` even though the installed binary implements it. A skill author reading only the help will assume there is no safe rehearsal mode and either skip validation or send real messages into live agent panes. FIX: `--dry-run` works today — always rehearse a target with `maw hey <target> "probe" --dry-run` (prints `dry-run: hey <t> -> local|self-node|peer <resolved>` and sends nothing) before the real call.
- Dotted pane targets are handed to tmux verbatim, so guessing indices fails. `<session>:1.1` errors with `tmux exited with status 1: can't find pane: 1` — on this fleet windows are index **1** and panes are index **0**, so the correct form is `<session>:1.0`. FIX: never hardcode `.1`; read the exact target from `maw ls -v`, `maw whoami`, or `tmux list-panes -a -F '#{session_name}:#{window_index}.#{pane_index}'`, or address the pane by its stable `%<id>` which peek also accepts.
- `maw team --help` / `maw team up --help` do NOT print help — they die with `team: unknown argument --help`, which a script may misread as 'verb does not exist'. FIX: invoke the subcommand with zero arguments (`maw team up`, `maw team spawn`) — it prints the usage line and exits with no side effects.
- `maw work` / `maw workon` have NO `--dry-run`. `maw work . --dry-run` just reprints the usage; there is no rehearsal path, and a correct invocation immediately creates a worktree + spawns a window. FIX: rehearse the worktree half separately (`maw worktree ls`, `git worktree list`) and treat the first real `maw work --wt` as a committing action.
- `maw swarm` has no worktree support whatsoever — `maw swarm codex codex codex` puts all three agents in one shared cwd. FIX: for isolated coders use `maw work --wt` per agent, or `maw team up` / `maw team spawn --worktree <path>`; reserve swarm for read-only or genuinely shared-context work.
- `maw kill` and `maw done` are not interchangeable and picking wrong destroys or strands work: `kill` removes the tmux session/window/pane immediately and leaves the worktree behind (orphan); `done` saves retro state, kills the window, AND removes the worktree; `sleep` stops the window gracefully and touches no worktree. FIX: worktree-backed agents must be retired with `maw done <window> --dry-run` first, then for real.
- `maw` on PATH is a zsh function, so `command -v maw` returns the bare string `maw` and `ls -la $(command -v maw)` fails. Non-interactive/`sh` contexts get the raw binary and lose `maw warp`. FIX: resolve the binary with `zsh -lc 'whence -p maw'` → /Users/nat/.local/bin/maw.
- The maw-rs checkout at /opt/Code/github.com/Soul-Brews-Studio/maw-rs (HEAD 3979e884) does NOT contain the installed build's commit fd82a9b — `git cat-file -t fd82a9b` → `fatal: Not a valid object name`. Source read there can be newer, older, or a different branch than what runs (e.g. send_federation.rs:599 already documents `--dry-run` while the shipped help does not). FIX: confirm runtime behavior against the binary (`strings`, `--dry-run` probes) rather than the checkout.
- `maw squad` is still on the dev/bun tier and warns `TS runs unsandboxed; ship tier = WASM`. FIX: build the skill on `maw team ...` (the WASM-tier surface), not `maw squad`.
- In this shell `rg` misbehaves with some flag forms — `rg --type ts ...` and `rg -l 'pat' <dir>/` came back as GNU-grep errors (`grep: unrecognized option --type`, `grep: crates/: Is a directory`), silently returning zero matches and faking a negative result. FIX: call `/opt/homebrew/bin/rg` by absolute path, and prefer `-e <pattern>` plus `--iglob` over `--type`.

### SOURCE: accounts
FINDINGS:
- Slot inventory (~/.codex-team/N/auth.json, auth_mode=chatgpt for all, plan=pro for all). Slot 1: codexsomkit@oraclenet.work, last_refresh=2026-07-22T12:28:30.190Z. Slot 2: codexsanchai@oraclenet.work, last_refresh=2026-07-21T23:17:58.169Z. Slot 5: kanawut.chunchom@gmail.com, last_refresh=2026-07-17T13:19:21.524Z. Slot 6: max.t@oraclenet.org, last_refresh=2026-07-22T12:18:27.366Z.
- Slots 3 and 4 have NO live auth.json — only `auth.json.stale-20260711` (a same-day bulk rename on 2026-07-11). Slot 3: codex.fantech@oraclenet.work, last_refresh=2026-07-02T07:52:29.449Z. Slot 4: codex-party-four@oraclenet.work, last_refresh=2026-07-07T02:55:46.179Z. Both are DEAD slots — codex would launch unauthenticated there.
- There is an undocumented 7th slot: ~/.codex-team/hermes/auth.json → codexsomkit@oraclenet.work, plan=pro, last_refresh=2026-07-18T03:43:20.473Z. This is the SAME ACCOUNT as slot 1 — a genuine double-binding of one Pro account under two CODEX_HOMEs.
- `~/.codex-team/.usage-cache.json` (fetchedAt 2026-07-25T12:09:26.935Z) independently confirms the live roster: it lists team/1, team/2, team/5, team/6, team/hermes and OMITS team/3 and team/4 — the refresher already treats 3 and 4 as non-existent.
- Quota headroom is near-total: every account reads 5h left=100 / 7d left=100 except team/5 (kanawut.chunchom@gmail.com) at usedPercent=3, left=97, resetAfter=281031s. Nothing is rate-limited; the constraint is wiring, not quota.
- `/bin/ps eww -A -o pid=,command=` yields 11 processes carrying a CODEX_HOME: 9 → /Users/nat/.codex-team/1, 1 → /opt/Code/github.com/laris-co/esphome-fw/.codex, 1 → my own grep (artifact, not a real process).
- The 9 'slot 1' processes are ENV INHERITANCE, not codex CLI. They are keyboxd (pid 4722), hermes gateway (59709, 59719), pm2 ProcessContainerForkBun (59770), mlx_stt_server.py (59671, 59979, 60359), web_app.py (59670), and a Python at 60331. Zero actual `codex` binaries are running on this machine right now.
- Root cause of the slot-1 fan-out: /Users/nat/.hermes/gateway-pm2.sh:30 and /Users/nat/.hermes/profiles/codex/gateway-pm2.sh:14 both hard-code `export CODEX_HOME=/Users/nat/.codex-team/1`. The pm2 daemon inherits it and leaks it to every unrelated child it forks (STT server, Chrome, web_app).
- The one repo-local CODEX_HOME in use — /opt/Code/github.com/laris-co/esphome-fw/.codex (pid 59660, pm2 bun fork) — contains only `prompts/` and `trust.json`. It has NO auth.json, so it maps to NO slot email and would run unauthenticated.
- 39 repo-local codex-home dirs exist under /opt/Code/github.com; only 7 carry an auth.json. Five of those are byte-level copies of SLOT 5's credential (identical last_refresh=2026-07-17T13:19:21.524Z): Soul-Brews-Studio/maw-rs/.codex, Soul-Brews-Studio/phd-citation-oracle/.codex, laris-co/muninn-oracle/.codex, nat-build-with-oracle/codex-fanout/codex-1/.codex, and slot 5 itself → all kanawut.chunchom@gmail.com.
- Two repo homes carry accounts that exist in NO slot at all — orphaned credentials outside the rotation pool: Soul-Brews-Studio/multi-agent-workflow-kit/.codex → soulbrews.forlife@gmail.com, plan=team (!), last_refresh=2025-11-01 (~9 months stale); laris-co/esphome-heating-visualizer/.codex → nat.wrw@gmail.com, plan=pro, last_refresh=2025-10-11.
- Soul-Brews-Studio/facebook-timeline-oracle/.codex-home is the only dir using the `.codex-home` naming convention; it also holds kanawut.chunchom@gmail.com but at an OLDER last_refresh (2026-07-09T13:17:03.965Z) than slot 5 — a stale fork of the same account.
- Session-activity proxy per slot (newest sessions/*.jsonl mtime): slot 1 = 178 sessions, newest 2026-07-07 20:11; slot 2 = 120, 2026-07-07 20:06; slot 3 = 191, 2026-07-07 19:43; slot 4 = 135, 2026-07-07 19:49; slot 5 = 166, 2026-07-15 06:45; slot 6 = 7, 2026-07-14 11:42; slot hermes = 0 sessions. Slots 1-4 all stopped together on 2026-07-07.
- IDLE: slots 2 and 6 are fully authenticated, at 100% quota, and bound to zero processes — slot 6 is the most untouched (only 7 sessions ever). Slot hermes is credentialed with 0 sessions but shares slot 1's account so it is not independent capacity.
- OVERSUBSCRIBED: account codexsomkit@oraclenet.work is bound to slot 1 + slot hermes + all 9 env-inheriting processes via two hard-coded gateway scripts. Account kanawut.chunchom@gmail.com is bound to slot 5 + 5 repo-local copies — one Pro account spread across 6 filesystem locations with no coordination.
- BROKEN: slots 3 and 4 are dead-but-present. Any tooling that enumerates ~/.codex-team/1..6 and assumes 6 usable slots will hand out two that cannot authenticate. Real usable slot count is 4 (1, 2, 5, 6), backed by 4 distinct accounts.
VERIFIED COMMANDS:
- ls -la ~/.codex-team/
- jq -r 'paths(scalars) | join(".")' ~/.codex-team/1/auth.json
- jq -r '"auth_mode=\(.auth_mode) last_refresh=\(.last_refresh)"' ~/.codex-team/1/auth.json
- /bin/ps eww -A -o pid=,command=
- /bin/ps -p <PID> -o command=
- /usr/bin/grep -o 'CODEX_HOME=[^ ]*' <ps-output-file> | /usr/bin/sort | /usr/bin/uniq -c | /usr/bin/sort -rn
- /usr/bin/find /opt/Code/github.com -maxdepth 5 -type d -name '.codex-home'
- /usr/bin/find /opt/Code/github.com -maxdepth 4 -type d -name '.codex'
- /usr/bin/grep -rn --include='*.sh' --include='*.toml' --include='*.json' 'CODEX_HOME' /Users/nat/.hermes/
- /usr/bin/stat -f '%Sm' -t '%Y-%m-%d %H:%M' <file>
- /usr/bin/python3 -c '<decode id_token payload, print only email + chatgpt_plan_type>'
PITFALLS:
- RTK hook silently mangles bare `ps`, `wc`, `find`, and `grep`. Bare `ps -A -o command=` returned 31 lines and `wc -l` reported 0 on a file that `grep -c` matched 11 times. Fix: always invoke absolute paths (/bin/ps, /usr/bin/grep, /usr/bin/wc, /usr/bin/find) for this audit, or `rtk proxy <cmd>`. `dangerouslyDisableSandbox: true` does NOT bypass the hook.
- The email and chatgpt_plan_type are NOT top-level fields in auth.json — the only top-level keys are auth_mode, last_refresh, tokens.{access_token,account_id,id_token,refresh_token}. Both values live in the id_token JWT payload: `email` and `https://api.openai.com/auth`.chatgpt_plan_type. Fix: base64url-decode segment [1] of the JWT in Python and print ONLY those two claims — never echo the token, and pad with '='*(-len(b)%4) or the decode raises.
- Counting raw CODEX_HOME hits in `ps eww` massively overstates usage. 9 of 11 hits on slot 1 are keyboxd, Chrome, an MLX STT server, and pm2 forks that merely inherited the var from the gateway — none is a codex process. Fix: cross-check with `/bin/ps -A -o command= | grep -E '(^| |/)codex( |$|-)'` (which finds zero live codex CLIs here) before calling a slot 'busy'.
- Dumping matched `ps eww` lines to show which process used which CODEX_HOME will print the whole environment block, including any OPENAI/GITHUB/HERMES token in it. Fix: capture pid + CODEX_HOME only via regex, then re-query the command separately with `/bin/ps -p <pid> -o command=` (no `e` flag) — never print the env line.
- Globbing ~/.codex-team/*/auth.json* in zsh aborts the loop with 'no matches found' when a slot has no stale file. Fix: `setopt NULL_GLOB`, or test `[ -f "$d/auth.json" ]` first and fall back to `/usr/bin/find "$d" -maxdepth 1 -name 'auth.json*'`.
- Assuming ~/.codex-team/1..6 is the full pool misses ~/.codex-team/hermes, which holds live credentials and duplicates slot 1's account. Fix: enumerate the directory, don't hard-code the 1..6 range; cross-check against the `accounts[].name` list in .usage-cache.json.
- Treating a repo-local CODEX_HOME as owning an account is wrong — esphome-fw/.codex has only prompts/ and trust.json, so codex there runs unauthenticated rather than falling back to a slot. Fix: check for auth.json existence before attributing a repo home to any slot email.
- Identifying which slot a repo-local copy came from cannot be done by email alone (5 dirs share kanawut.chunchom@gmail.com). Fix: use last_refresh as the discriminator — exact matches (2026-07-17T13:19:21.524Z) are copies of slot 5; an older value (2026-07-09T13:17:03.965Z in facebook-timeline-oracle) is a stale earlier fork.
- `find ... -name A -o -maxdepth N -name B -type d` applies -type d only to the last branch and warns about -maxdepth placement, silently returning files as well as dirs. Fix: run one clean pass per name with `-type d` before `-name`.