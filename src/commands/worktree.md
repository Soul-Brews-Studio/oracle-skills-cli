---
description: v1.5.59 | Git worktree for parallel work. Use when user says "worktree", "parallel work", "new agent", "start parallel".
---

# /worktree

Execute the `worktree` skill with the provided arguments.

## Instructions

**If you have a Skill tool available**: Use it directly with `skill: "worktree"` instead of reading the file manually.

**Otherwise**:
1. Read the skill file: `{skillPath}/worktree/SKILL.md`
2. Follow all instructions in the skill file
3. Pass these arguments to the skill: `$ARGUMENTS`

## Skill Location

- Local: `.claude/skills/worktree/SKILL.md`
- Global: `~/.claude/skills/worktree/SKILL.md`

---
*oracle-skills-cli v1.5.59*
