---
name: calibrate
description: Calibrate Oracle instruments. Alias for /soul-sync. Use when user says "calibrate", "tune", or before /awaken.
---

# /calibrate

> Sugar alias for `/soul-sync`

**Just run `/soul-sync`** — this is for those who prefer the calibration metaphor.

```
/soul-sync   # Main (recommended)
/calibrate   # Alias (same thing)
/update      # Alias (same thing)
```

See `/soul-sync` for complete documentation.
